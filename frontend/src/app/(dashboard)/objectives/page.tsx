"use client";

import { SectionHeaderButton } from "@/components/custom/sectionHeader";

export default function Objectives() {
  return (
    <div>
      <section>
        <SectionHeaderButton text="Overview" onClick={() => {}} />
      </section>

      <section>
        <SectionHeaderButton text="Objectives" onClick={() => {}} />
      </section>
    </div>
  );
}
